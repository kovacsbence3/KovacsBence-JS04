const lista = [];

document.addEventListener("DOMContentLoaded", () => ){
    const urlap = document.forms("urlap")
    urlap.addEventListener("submit", (event) => {
        event.preventDefault();
        const nev = urlap["urlap"]["nev"];

    })
    
};

